# Block outgoing IPv6 traffic that bypasses the VPN tunnel.
# VPN traffic on utun interfaces is already allowed by 200.allowVPN (quick),
# so this rule only affects non-tunnel IPv6 traffic (leak prevention).
block return out inet6 flags any no state
