name: vscode-launch-config
summary: Generate or update `.vscode/launch.json` for the Amnezia Client workspace.
scope: workspace

When invoked, use the repository layout and existing `.vscode/launch.json` to produce or improve VS Code debug launch configurations for this project.

Key goals:
- Preserve or update the existing Chrome launch configuration for `http://localhost:8080`.
- Add or fix C++/Qt debugging configurations for the main Amnezia client application built by CMake.
- Prefer launch configurations that work on Linux using the project build output and the default debugger available in VS Code.
- Use repository paths relative to the workspace root and include clear comments for each configuration entry.

Example invocation:
- "Generate a VS Code launch.json for the Amnezia Client project."
- "Add Linux C++/Qt debug launch settings to `.vscode/launch.json`."

If the project requires a build directory path, explain the assumption and the expected location of the built executable.