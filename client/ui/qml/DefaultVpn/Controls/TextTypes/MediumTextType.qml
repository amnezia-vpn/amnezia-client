import QtQuick

import Config 1.0

Text {
    lineHeight: 22
    lineHeightMode: Text.FixedHeight

    color: Style.color.black
    font.pixelSize: 17
    font.weight: 400
    font.family: Style.font

    wrapMode: Text.WordWrap
}
