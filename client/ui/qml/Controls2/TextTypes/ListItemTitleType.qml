import QtQuick

Text {
    lineHeight: 21.6
    lineHeightMode: Text.FixedHeight

    color: "#D7D8DB"
    font.pixelSize: 18
    font.weight: 400
    font.family: "PT Root UI VF"

    wrapMode: Text.Wrap
}
