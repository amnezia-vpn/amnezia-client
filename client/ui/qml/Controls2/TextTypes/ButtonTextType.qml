import QtQuick

Text {
    lineHeight: 24
    lineHeightMode: Text.FixedHeight

    color: "#D7D8DB"
    font.pixelSize: 16
    font.weight: 500
    font.family: "PT Root UI VF"

    wrapMode: Text.WordWrap
}
